# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Firebase keys. .env.local is covered above; a bare .env is not.
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# vite-plugin-pwa dev output
dev-dist

# The og card, assembled from the template and the component. Regenerate
# with `npm run og`; only public/og.png is shipped.
assets/og.built.svg

# Claude Code workspace settings, local to whoever is working here.
.claude/
