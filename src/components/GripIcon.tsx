/**
 * The handle: two rules, the same ink outline as everything else. Small
 * enough to read as something to hold rather than something to press.
 */
export function GripIcon() {
  return (
    <svg viewBox="0 0 24 24" aria-hidden="true">
      <path d="M5 9.5h14" />
      <path d="M5 14.5h14" />
    </svg>
  );
}
